<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rgbremote.euphoriawifi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity
            android:name=".tabNav"
            android:label="@string/title_activity_tab_nav"
            android:theme="@style/SplashTheme.NoActionBar"></activity>
        <activity android:name=".gotobed2" />
        <activity android:name=".gotobed1" />
        <activity android:name=".wake_up2" />
        <activity android:name=".wake_up1" />
        <activity android:name=".bottomNavPage" />
        <activity android:name=".Congrats" />
        <activity android:name=".captured" />
        <activity android:name=".Setup" />
        <activity android:name=".productPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>